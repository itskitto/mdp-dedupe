# PostgreSQL Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=mdp_dedupe
DB_USER=your_username
# DB_PASSWORD is optional when using Postgres.app on Mac
# DB_PASSWORD=your_password

# Optional: Set to true to see SQL queries in logs
# DB_ECHO=true
